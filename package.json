{
  "name": "Shawns_Blog",
  "version": "0.0.1",
  "private": true,
  "description": "Blog",
  "scripts": {
      "build": "node build.js",
      "deploy": "npm run build && cd build && git init . && git add . && git commit -m \"Deploy\"; git push \"git@github.com:jaegerpicker/jaegerpicker.github.io.git\" master:master --force && rm -fr .git"
  },
  "author": "Shawn Campbell <jaegerpicker@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "highlighter": "^0.1.0",
    "metalsmith": "^2.3.0",
    "metalsmith-collections": "^0.9.0",
    "metalsmith-define": "^2.0.1",
    "metalsmith-markdown": "^0.2.1",
    "metalsmith-pagination": "^1.4.0",
    "metalsmith-permalinks": "^0.5.0",
    "metalsmith-snippet": "^2.0.0",
    "metalsmith-templates": "^0.7.0",
    "moment": "^2.17.1"
  }
}
